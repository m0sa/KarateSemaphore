<Grid x:Class="KarateSemaphore.Phone.TimeControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:core="clr-namespace:KarateSemaphore.Core"
    mc:Ignorable="d"
    d:DataContext="{d:DesignInstance core:StopWatchViewModel, IsDesignTimeCreatable=True}"
    d:DesignHeight="80" d:DesignWidth="400"
    >
    <Grid.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="Style.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Grid.Resources>
    <Viewbox>
        <TextBlock 
            Style="{StaticResource ResourceKey=panelText}"
            Text="{Binding 
                Remaining, 
                Mode=OneWay, 
                Converter={StaticResource TimeDisplayConverter}}" />
    </Viewbox>
    <Button Command="{Binding StartStop}" Content="" BorderThickness="0">
  </Button>

</Grid>